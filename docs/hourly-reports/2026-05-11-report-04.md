2026-05-11 23:31:24 +02:00

## Report
- Replaced the early Report 04 draft. The previous file was written at 22:39, too close to Report 03 at 22:31, so this is the valid fourth hourly report.
- Added persistent manager review state to `bayaan.shift.close`: pending/approved/rejected, manager note, reviewer, review timestamp, and investigation status.
- Added `/bayaan/api/shift_close_review` so Daily Close approve/reject/note actions write to the backend audit layer instead of living only in frontend state.
- Expanded `/bayaan/api/chain_bootstrap` with aggregate command-center totals, payment-method splits, true source counts, per-kiosk rollups, recipe posting issue counts, and daily/weekly/monthly/yearly report-period aggregates.
- Tightened report-period payment aggregation so it excludes draft/cancelled POS orders just like the daily payment domain.
- Wired Daily Close to show close metadata, notes/investigation status, recipe posting review, and the full variance input equation: opening + received - recipe consumption - waste = expected.
- Wired Reports to read aggregate `summary.reportPeriods` data and added a traceable CSV export pack for revenue, COGS, waste, cash, digital payments, and source row counts.
- Cleaned the active exact-design runtime of visible Arabic mojibake and fixed remaining runtime regressions where fallback operators had been damaged.
- Wired Products & Recipes so recipe coverage counts include live/bootstrap recipe versions, product rows show engine recipe badges, and Save submits `/bayaan/api/recipe_version` when the backend engine is configured.
- Expanded smoke coverage for Daily Close review actions, Products & Recipes editor open, Arabic RTL/no-mojibake, no visible Odoo branding, and a new Products & Recipes screenshot.
- Updated `docs/admin-dashboard-concept.md` with the report export, recipe-version write contract, and frontend verification contract.

## Verify
- `apps/kiosk-pos`: `npm run verify` passed at 23:23-23:24.
  - Vitest: 4 files / 14 tests passed.
  - TypeScript + Vite build passed.
  - Playwright smoke passed and refreshed screenshots, including `exact-admin-products-recipes.png`, `exact-admin-daily-close.png`, `exact-admin-reports.png`, and `exact-arabic-pos.png`.
- Backend syntax check passed: `python -m py_compile backend/.../controllers/api.py backend/.../models/bayaan_shift_close.py`.
- Cleaned generated `__pycache__` folders after backend compile.
- Residual note: Vite still reports the existing non-blocking main chunk size warning.

## Next Plan
- Build the next operational action layer in Stock & Allocation: convert suggested transfers into a manager action that drafts a real warehouse-to-kiosk internal transfer through `/bayaan/api/stock_transfer`.
- Preserve demo-safe behavior when `VITE_ODOO_URL` is unset, but make the connected mode write to the single backend database.
- Add smoke coverage for the visible transfer action without requiring a live backend.
- Produce Report 05 after the next valid hourly boundary, after 2026-05-12 00:31:24 +02:00.
