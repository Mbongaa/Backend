2026-05-12 00:31:50 +02:00

## Report
- Added an actionable Stock & Allocation flow: each "Suggested transfers for tomorrow" row now has a "Create transfer" action.
- Preserved raw suggestion fields (`kioskId`, `itemId`, numeric quantity, UoM) so the UI can call the source-of-truth gateway instead of parsing display text.
- Wired connected mode to `sourceOfTruth.submitStockTransfer()`, which posts to `/bayaan/api/stock_transfer`; demo mode remains safe and shows the same operational feedback without requiring Odoo.
- Added immediate local feedback after drafting a suggested transfer: the Pending transfers list receives a new draft row such as `DRAFT-K-04-oat-milk-1l`.
- Tightened the backend `/bayaan/api/stock_transfer` route to honor the requested UoM when creating the stock move, falling back to the product default when needed.
- Strengthened smoke coverage:
  - clicks "Create transfer";
  - verifies the "Draft transfer prepared" toast;
  - verifies the new draft transfer row appears;
  - captures `verification/exact-admin-stock-transfer-draft.png`.
- Added smoke coverage for the Reports CSV export path by clicking "Export pack" and verifying the export toast.
- Added a small scroll-to-top helper in smoke so screenshots, especially Reports, are captured from the top after interactive actions.
- Updated `docs/admin-dashboard-concept.md` so Stock & Allocation describes suggested transfers as actionable backend stock movements.
- Found and paused the stale cron automation that was generating premature/incorrect report drafts. Removed the invalid early Report 05 draft so the directory now contains only valid hourly reports.

## Verify
- `apps/kiosk-pos`: `npm run verify` passed at 2026-05-12 00:20-00:21.
  - Vitest: 4 files / 14 tests passed.
  - TypeScript + Vite build passed.
  - Playwright smoke passed with refreshed screenshots, including `exact-admin-stock-transfer-draft.png`.
- Backend syntax check passed: `python -m py_compile backend/.../controllers/api.py backend/.../models/bayaan_shift_close.py`.
- Cleaned generated `__pycache__` folders after backend compile.
- Verified the report automation is paused and no early Report 05 file remains.
- Residual note: Vite still reports the existing non-blocking main chunk size warning.

## Next Plan
- Stop here for the minimum requested five hourly reports unless the session is explicitly continued.
- If continued, the next best product slice is purchase-order action wiring: turn below-reorder purchase recommendations into draft PO actions without allowing zero-cost or untraceable supplier pricing.
