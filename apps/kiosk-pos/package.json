{
  "name": "kiosk-pos",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5174 --strictPort",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run --pool threads",
    "smoke": "node scripts/smoke.mjs",
    "smoke:live": "node scripts/live-odoo-smoke.mjs",
    "gate:wiring": "node scripts/wiring-gate.mjs",
    "verify": "npm test && npm run gate:wiring && npm run build && npm run smoke",
    "verify:live": "npm run verify && npm run smoke:live"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "playwright": "^1.59.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "lottie-web": "^5.13.0",
    "lucide-react": "^1.14.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  }
}
